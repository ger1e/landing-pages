--- a/index.html
+++ b/index.html
@@ -143,10 +143,11 @@
   .cas-clock{width:151px;height:19px;padding:0 6px;gap:4px}.cas-date{font-size:6.7px}.cas-time{font-size:8.2px}
   .portrait-link{top:26px;width:100vw;height:calc(var(--mobile-stable-h) * .618 - 26px)}
   .portrait-base,.portrait-slice{object-position:50% 5%;transform:scale(1.015)}
-  .matrix{inset:26px 0 0 0;height:calc(100svh - 26px)}
+  #workstation{top:0;left:0;right:0;bottom:auto;height:var(--mobile-stable-h);min-height:var(--mobile-stable-h)}
+  .matrix{inset:26px 0 0 0;height:calc(var(--mobile-stable-h) - 26px)}
   .terminal{left:max(8px,2vw,env(safe-area-inset-left));right:max(8px,2vw,env(safe-area-inset-right));width:auto;top:calc(var(--mobile-stable-h) * .63);bottom:auto;height:calc(var(--mobile-stable-h) * .36);max-height:calc(var(--mobile-stable-h) * .36);grid-template-rows:28px minmax(0,1fr) auto;font-size:clamp(11px,2.9vw,12.5px);line-height:1.46}
-  #workstation.mobile-keyboard .terminal{top:calc(var(--vvh) * .58);height:calc(var(--vvh) * .40 - max(6px,env(safe-area-inset-bottom)));max-height:none}
-  #workstation.mobile-keyboard .wordmark{opacity:0;pointer-events:none}
+  /* Keyboard may pan the terminal as one rigid panel; its dimensions never change. */
+  #workstation.mobile-keyboard .terminal{transform:translate3d(0,calc(var(--vvh) - (var(--mobile-stable-h) * .99) - max(6px,env(safe-area-inset-bottom))),0)}
   .capture-input{font-size:16px!important}
   .term-titlebar{height:28px;padding:0 9px;font-size:8.5px;gap:7px}.ibm-logo{width:34px;height:14px}.brand-wrap{min-width:50px;gap:6px}.term-body{padding:10px 12px 8px}.input-row{min-height:40px;padding:8px 12px}
   .wordmark{right:7px;bottom:calc(var(--mobile-stable-h) * .42);width:min(96vw,420px);height:clamp(88px,8.2svh,126px);transform-origin:100% 82%;display:flex;align-items:flex-end;justify-content:flex-end;animation:wordExpandMobile 16s cubic-bezier(.38,0,.2,1) infinite}
@@ -164,7 +165,8 @@
   .custom-cursor{display:none!important}
 }
 @media (prefers-reduced-motion:reduce){
-  .wordmark,.block-cursor{animation:none!important}.portrait-wrap.glitching .portrait-base,.portrait-wrap.glitching .portrait-slice,.portrait-wrap.glitching .portrait-tear,.wordmark.glitching .wm-slice,.wordmark.glitching::before,.wordmark.glitching::after,.wordmark.glitching .wordmark-link::before,.wordmark.glitching .wordmark-link::after,.term-title.glitching,.workstation-tear,.sync-lines.glitching .sync-line{animation:none!important}.matrix{opacity:.18!important}
+  /* Keep user-requested short fault/glitch events functional; suppress only ambient looping motion. */
+  .wordmark:not(.glitching),.block-cursor{animation:none!important}
 }
 </style>
 </head>
@@ -215,8 +217,9 @@
   </section>
 
   <div class="custom-cursor" id="customCursor" aria-hidden="true"><svg viewBox="0 0 22 26" xmlns="http://www.w3.org/2000/svg"><path d="M2 1L19 15l-8 .8 4.2 8.1-4.2 2-4-8-5 5z" fill="#eef9ff" stroke="#001436" stroke-width="1.35" stroke-linejoin="miter"/><path d="M6.2 5.1L9.4 17" stroke="#0f62fe" stroke-width="1.35"/></svg></div>
-  <div id="spotifyEmbed" style="position:fixed;width:1px;height:1px;left:-20px;top:-20px;overflow:hidden;opacity:.001;pointer-events:none" aria-hidden="true"></div>
+  <div id="spotifyEmbed" style="position:fixed;width:320px;height:80px;left:-10000px;top:0;overflow:hidden;opacity:.001;pointer-events:none" aria-hidden="true"></div>
 </main>
+<!-- FINAL-BOUNDED-HARDENING-2026-08-21 -->
 <script>
 (()=>{
 'use strict';
